from django.contrib import admin
from django.urls import path, include
from . import views

urlpatterns = [
   #path('', views.BenchmarkModelList.as_view()),
   #path('<int:pk>/', views.BenchmarkModelDetail.as_view()),
]
